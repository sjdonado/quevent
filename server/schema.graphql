type Query {
  getUser: User!
}

type Mutation {
  login(idToken: String!): AuthPayload!
}

type AuthPayload {
  token: String!
  user: User!
}

type User {
  name: String!
  email: String!
  profilePicture: String!
  active: Boolean!
  attended: Boolean!
}
